import 'package:flutter/material.dart';

class ForSale extends StatelessWidget {
  const ForSale({Key? key}) : super(key: key);
  static const routeName = 'forsale';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
